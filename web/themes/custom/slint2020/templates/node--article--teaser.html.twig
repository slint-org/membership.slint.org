{#
/**
 * @file
 * SLINT2020's theme implementation to display an Article node in teaser view mode.
 */
#}
{%
  set classes = [
    'node',
    'node--type-' ~ node.bundle|clean_class,
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
    view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
    display_submitted ? 'node--display-submitted',
    'clearfix',
  ]
%}
{{ attach_library('slint2020/node-view-mode-teaser') }}
{{ attach_library('slint2020/node-field') }}
{% set image = content.field_image %}
{% set teaser_pre_content = content.field_tags %}
{% set rendered_content = content|without('field_image', 'field_tags') %}
{% set submitted_date = node.createdtime|format_date('custom', 'd M Y') %}
{% include '@slint2020/partials/_mt-teaser-2.html.twig' %}
